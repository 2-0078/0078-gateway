version: "3.8"

services:
  gateway:
    container_name: gateway
    image: ${DOCKER_USERNAME}/gateway:latest
    ports:
      - "8000:8000"
    env_file:
      - .env
    networks:
      - backend

networks:
  backend:
    external: true