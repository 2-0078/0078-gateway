version: "3.8"

services:
  gateway:
    image: eunseo0305/gateway:latest
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - backend

networks:
  backend:
    driver: bridge